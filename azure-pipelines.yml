trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- script: |
    echo "Azure DevOps Pipeline Running"
    echo "Current directory:"
    pwd
    echo ""
    echo "Repository contents:"
    ls -la
  displayName: 'Verify Pipeline'

- script: |
    git --version
  displayName: 'Verify Git'

- script: |
    curl --version
  displayName: 'Verify Curl'
